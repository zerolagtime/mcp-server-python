name: Python MCP
version: 0.0.2
schema: v1
mcpServers:
  - name: Python MCP
    command: docker
    args:
      - run
      - --rm
      - -i
      - --network
      - none
      - python-mcp
    env: {}