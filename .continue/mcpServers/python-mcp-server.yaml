name: Python MCP
version: 0.2.0
schema: v1
mcpServers:
  - name: Python MCP
    command: docker
    args:
      - run
      - --rm
      - -i
      - --network
      - none
      - python-mcp
    env: {}